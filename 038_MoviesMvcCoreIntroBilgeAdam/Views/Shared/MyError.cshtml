@model string

@* Projedeki tüm controller ve view'lar üzerinden paylaşılarak kullanılabilmesi için bu view ~/Views/Shared klasörü altında oluşturulmuştur. *@

@{
    string message = string.IsNullOrWhiteSpace(Model) ? "An error occurred." : Model;
}

<h2 class="text-danger">Error!</h2>
<p class="text-danger">@message</p>